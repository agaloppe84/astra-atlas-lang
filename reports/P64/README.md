# ASTRA-P64 Results

This directory is reserved for future ASTRA-P64 Results `.tex` and `.pdf`
artifacts.

P64-1 currently keeps the durable scientific trace in:

- `docs/analysis/ASTRA-P64-address-local-realish-ratio-analysis.md`

The final Results report should be generated only after the address-local
realish campaign model is stabilized. CI remains minimal and does not compile
heavy reports.
