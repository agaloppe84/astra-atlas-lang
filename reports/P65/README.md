# ASTRA-P65 Results

This directory is reserved for future ASTRA-P65 Results `.tex` and `.pdf`
artifacts.

P65-1 currently keeps the durable scientific trace in:

- `docs/analysis/ASTRA-P65-address-local-actor-runtime-analysis.md`

The final Results report should be generated after the local actor overhead
model is stabilized. CI remains minimal and does not compile heavy reports.
